
**Задание:**
- Реализовано моделирования возникновения и распространения лесных пожаров с использованием автомата Мура. GUI есть.
**Требования:**
- реализовать **не менее трёх дополнительных правил** поведения системы.
- Есть лиственные и хвойные деревья, у разного типа разные реакции на огонь.
 Хвойные деревья загораются с большей вероятностью чем лиственные, а старые с еще большей вероятностью
 - 2-Мол.Листва, 0.20
 - 3-Ст.Листва, 0.28
 - 4-Мол.Хвоя,0.3
 - 5-Ст.Хвоя 0.5
- Огонь горит несколько шагов.
-  if state == 1:
   if np.random.rand() < 0.2:
   nxt[y, x] = 0
   Чтобы сгореть необходимо чтобы выпало число меньшее 0.2, в таком случае клетка огня превратится в пустырь, иначе заново проверяется на следующем шаге, интерпретировать это можно как количество времени которое будет гореть дерево.
 
- Пустыри зарастают.
elif state == 0:
 if np.random.rand() < 0.0002:
  nxt[y, x] = 2
  С вероятностью 2/10000 на месте пустыря может вырасти молодое дерево
- Точки возгорания выбираются самостоятельно, количество неограниченное.
- Точка возгараниия задается пользователем, может быть несколько точек, точка возгарания = удар молнии, может так же поджечь территорию вокруг клетки
